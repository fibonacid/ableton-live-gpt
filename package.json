{
  "name": "ableton-live-gpt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --external:max-api --target=node20 --outfile=code/main.js",
    "watch": "yarn build --watch=forever",
    "start": "open ./patchers/main.maxpat",
    "dev": "yarn watch & yarn start",
    "format": "prettier --write src/**/*.ts"
  },
  "engines": {
    "node": ">=20.6"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.3",
    "@types/max-api": "^2.0.3",
    "@types/node": "^20.11.15",
    "esbuild": "^0.20.0",
    "osc-js": "^2.4.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/node-osc": "^6.0.3",
    "openai": "^4.26.0"
  }
}
